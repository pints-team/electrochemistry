# Pints: Electrochemistry

## To Install 

- install pints
- make sure you get submodules (pybind11 and eigen)
```bash
    $ git submodule init
    $ git submodule update
```
- install with pip
```bash
    $ pip install -e .
```
